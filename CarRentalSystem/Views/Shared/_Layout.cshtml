<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-dark bg-dark">
        <div class="container">
            @* @Html.ActionLink("Car Rental Services", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })*@
            <a href="@Url.Action("Index", "Home", new { area = "" })" class="navbar-brand">
                <i class='fas fa-car' style='font-size:21px'></i> Car Rental System
            </a>
           
            <div class="collapse navbar-collapse d-sm-inline-flex justify-content-between">
                <ul class="navbar-nav flex-grow-1">
                    <li>@ViewBag.Login</li>
                    <li>@ViewBag.Car</li>
                    <li>@ViewBag.Customer</li>
                    <li>@ViewBag.Rent</li>
                    @{
                        var isLoggedIn = Session["UserName"] != null;
                    }
                    @if (isLoggedIn)
                    {
                        <li class="nav-item">
                            <a href="@Url.Action("Logout", "Login")" class="nav-link text-white fw-bold border border-white rounded-pill px-3"><i class="fas fa-sign-out-alt me-2"></i>Logout</a>
                        </li>
                    }
                    @*<li>@Html.ActionLink("Login", "UserLogin", "Login", new { area = "" }, new { @class = "nav-link", style = "font-size:18px; color: white; display: inline-block; " })</li>
        <li>@Html.ActionLink("Customer Details", "Index", "User", new { area = "" }, new { @class = "nav-link", style = "font-size:18px; color: white; display: inline-block;  margin-right: 10px; " })</li>
        <li>@Html.ActionLink("Car Details", "Index", "Car", new { area = "" }, new { @class = "nav-link", style = "font-size:18px; color: white;display: inline-block;  margin-right: 20px; " })</li>*@
                </ul>


                @*@if (Session["LoggedIn"] == null)
            {
                <li>@Html.ActionLink("Login", "UserLogin", "Login", new { area = "" }, new { @class = "nav-link", style = "font-size:18px; color: white;display: inline-block;  margin-right: 20px; "  })</li>

            }
            else
            {
            <li>@Html.ActionLink("Car List", "Index", "Car", new { area = "" }, new { @class = "nav-link" })</li>
            <li>@Html.ActionLink("Customer List", "Index", "User", new { area = "" }, new { @class = "nav-link" })</li>
            <li>@Html.ActionLink("CarRentalSystem ", "Index", "Car", new { area = "" }, new { @class = "nav-link" })</li>


            }*@





            </div>
        </div>
    </nav>
    <div class="container body-content">
        @RenderBody()
        <hr />
       
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
